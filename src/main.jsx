import React from 'react'
import ReactDOM from 'react-dom/client'
import { OpenWeatherApp } from './components/OpenWeatherApp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <OpenWeatherApp />
  </React.StrictMode>,
)
